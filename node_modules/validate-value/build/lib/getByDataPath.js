"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getByDataPath = void 0;
const getByDataPath = function ({ object, dataPath }) {
    const pathSegments = dataPath.replace(/\//ug, '.').split('.');
    const nonEmptyPathSegments = pathSegments.filter((segment) => segment !== '');
    let value = object;
    for (const pathSegment of nonEmptyPathSegments) {
        if (value === undefined) {
            break;
        }
        value = value[pathSegment];
    }
    return value;
};
exports.getByDataPath = getByDataPath;
